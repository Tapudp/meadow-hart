import responses from './responses';

const chatBoxId = 'chat-box';

const SIMULATED_RESPONSE_DELAY = 2500;

const constants = {
  chatBoxId,
  responses,
  SIMULATED_RESPONSE_DELAY,
};

export default constants;
